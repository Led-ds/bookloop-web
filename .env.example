# URL base da API consumida pelo frontend. É embutida no build (Vite).
# Desenvolvimento: normalmente aponta para a API local.
# Produção: URL pública da API (App Runner).
VITE_API_URL=http://localhost:8080/api/v1
